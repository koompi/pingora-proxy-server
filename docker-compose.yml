# docker-compose.yml with persistent configuration and Cloudflare credentials
version: "3.7"

services:
  proxy:
    image: image.koompi.org/library/pingora-proxy-server:latest
    dns:
      - 127.0.0.11
      - 8.8.8.8
      - 1.1.1.1
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    networks:
      - proxy-network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /mnt/gluster/certbot:/certbot
      - /mnt/gluster/acme-challenge:/var/www/html/.well-known/acme-challenge
      - /mnt/gluster/proxy-config:/app/config # Persistent configuration directory
      - /mnt/gluster/pingora-proxy/locks:/pingora-proxy/locks # Shared lock directory
      - /tmp:/tmp
      - /var/cache:/var/cache
    environment:
      - SWARM_MODE=false
      - SWARM_NETWORKS=ingress
      - LOG_LEVEL=info
      - CONFIG_PATH=/app/config/config.json # Point to the new location
      # Let's Encrypt configuration
      - LETS_ENCRYPT_EMAIL=honghangsia@gmail.com
      # Cloudflare credentials for wildcard certificates
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-}
      - DISABLE_SSL=false
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: any
        delay: 5s
    configs:
      - source: proxy-security-policy
        target: /etc/security-policy.json
        mode: 0644

networks:
  proxy-network:
    external: true

configs:
  proxy-security-policy:
    file: ./security-policy.json
